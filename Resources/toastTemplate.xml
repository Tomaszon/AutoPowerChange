<toast>
	<visual>
		<binding template="ToastGeneric">
			<text hint-maxLines="1">{toastHeader}</text>
			<text>{content}</text>
			<image placement="appLogoOverride" hint-crop="circle" src="{image}"/>
		</binding>
	</visual>

	<actions>
		<action activationType="background" arguments="dismiss" content="Dismiss" imageUri="{resourceFolder}\dismiss.png"/>
		{resolutionButton}
		<action activationType="protocol" arguments="apc://{currentLocation}\Invoke-PowerPlanRestore.ps1 {previousPlanName} {previousPlanGuid}" content="Restore plan" imageUri="{resourceFolder}\restore.png"/>
	</actions>
</toast>