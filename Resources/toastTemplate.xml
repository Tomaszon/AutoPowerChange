<toast>
	<visual>
		<binding template="ToastGeneric">
			<text hint-maxLines="1">{toastHeader}</text>
			<text>{content}</text>
			<image placement="appLogoOverride" hint-crop="circle" src="{icon}"/>
		</binding>
	</visual>

	<actions>
		<action activationType="background" arguments="dismiss" content="Dismiss" />
		<action activationType="protocol" arguments="apc://{currentLocation}\Invoke-ScreenResolutionChange.ps1" content="Change resolution" />
		<action activationType="protocol" arguments="apc://{currentLocation}\Invoke-PowerPlanRestore.ps1 {previousPlanGuid} {previousPlanName}" content="Restore plan" />
	</actions>
</toast>